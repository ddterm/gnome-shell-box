on:
  workflow_dispatch:
  push:
    branches:
      - master

concurrency: ${{ github.workflow }}/${{ github.ref }}

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      publish: false
